# Brevo アカウント作成 - 住所なしセットアップガイド

Brevo でメール配信を設定する際、**住所を入力したくない場合**の方法を説明します。

---

## ✅ 住所は必須ではありません

Brevo は個人ユーザーの場合、**住所を入力しなくても問題ありません**。以下の手順で、住所を入力せずにアカウント作成できます。

---

## 📋 Brevo アカウント作成手順（住所なし）

### ステップ1：Brevo にアクセス

1. ブラウザで以下の URL を開く：
   ```
   https://www.brevo.com
   ```

2. 右上の「Sign up」または「無料で始める」をクリック

### ステップ2：メールアドレスで登録

1. **メールアドレス**を入力
   - 例：your-email@gmail.com

2. **パスワード**を設定
   - 8文字以上の強力なパスワード

3. **利用規約に同意**にチェック

4. 「Create my account」をクリック

### ステップ3：メール認証

1. 登録したメールアドレスに確認メールが届く

2. メール内の「Verify your email」リンクをクリック

3. メール認証が完了

### ステップ4：アカウント情報の入力（住所は入力しない）

1. **First name**（名前）：入力
   - 例：Taro

2. **Last name**（苗字）：入力
   - 例：Yamada

3. **Company name**（会社名）：入力（オプション）
   - 個人の場合は空白でも OK
   - または「Personal」と入力

4. **Country**（国）：「Japan」を選択

5. **Phone number**（電話番号）：入力（オプション）
   - 入力しなくても OK

6. **Address**（住所）：**入力しない**
   - このフィールドは空白のままにする
   - 必須ではないため、スキップ可能

7. 「Continue」をクリック

### ステップ5：プランを選択

1. **Free プラン**を選択（無料）
   - 月300件のメール送信
   - 登録者 500 人まで無料

2. 「Start with Free」をクリック

### ステップ6：ダッシュボードにアクセス

1. Brevo ダッシュボードが表示される

2. 左側メニューから「Contacts」を選択

3. 「Create a list」をクリックしてメールリストを作成

---

## 🔑 API キーの取得方法

メール登録フォームを自動化するために、API キーが必要です。

### ステップ1：Settings にアクセス

1. ダッシュボード右上の「Settings」をクリック

2. 左側メニューから「API」を選択

### ステップ2：API キーを生成

1. 「Create a new API key」をクリック

2. キーの名前を入力
   - 例：「AI Stock Media Roadmap」

3. 「Generate」をクリック

4. **API キーをコピー**
   - 後で使用するため、安全に保管

### ステップ3：API キーを設定

1. サイトの `NewsletterSignup.tsx` ファイルを編集

2. 以下の行を修正：
   ```typescript
   'api-key': 'YOUR_BREVO_API_KEY', // ← ここに API キーを貼り付け
   ```

3. 例：
   ```typescript
   'api-key': 'xkeysb1234567890abcdefghijklmnop',
   ```

---

## ⚠️ 重要な注意点

### 1. API キーは絶対に公開しない
- GitHub に API キーをコミットしない
- フロントエンドコードに直接記載しない
- バックエンド経由でリクエストを送信する（推奨）

### 2. 本番環境での安全な実装
フロントエンドから直接 API を呼び出すのは安全ではありません。以下の方法が推奨されます：

**方法1：バックエンド経由（推奨）**
```
フロントエンド → バックエンド → Brevo API
```

**方法2：Zapier を使用**
- Zapier で RSS → Brevo メール配信を自動化
- API キーを管理する必要がない

**方法3：Brevo 埋め込みフォーム**
- Brevo の埋め込みコードを使用
- API キーを公開しない

---

## 🚀 次のステップ

### 今すぐできること

1. **Brevo アカウント作成**（住所なし）
   - 上記の手順に従ってアカウント作成
   - メール認証を完了

2. **メールリスト作成**
   - 「Contacts」→「Create a list」
   - リスト名：「AI Stock Media Roadmap」

3. **API キー取得**
   - Settings → API → API キー生成
   - キーを安全に保管

### 本番環境での実装

1. **バックエンド実装**
   - Node.js/Express でメール登録エンドポイントを作成
   - API キーをサーバー側で管理

2. **フロントエンド修正**
   - バックエンド API を呼び出すように修正
   - API キーをフロントエンドに記載しない

3. **テスト**
   - メール登録フォームをテスト
   - メールが正常に受信されるか確認

---

## 💡 代替案：Zapier を使用（最も簡単）

API キーを管理したくない場合は、**Zapier で RSS → Brevo メール配信を自動化**するのが最も簡単です。

1. Zapier で新しい Zap を作成
2. Trigger：RSS by Zapier「New Item in Feed」
3. Action：Brevo「Add Contact」
4. メール配信を自動化

この方法なら、API キーを公開する必要がありません。

---

## ❓ よくある質問

**Q：住所を入力しないと、メール配信ができない？**
A：いいえ。住所は不要です。メール配信は正常に機能します。

**Q：API キーを失くしたらどうする？**
A：新しい API キーを生成できます。古いキーは無効化されます。

**Q：個人情報を最小限にしたい場合は？**
A：会社名、電話番号、住所は全て入力しなくても問題ありません。メールアドレス、名前、国だけで十分です。

---

## 🔒 セキュリティのベストプラクティス

1. **API キーは環境変数で管理**
   ```
   BREVO_API_KEY=xkeysb1234567890abcdefghijklmnop
   ```

2. **フロントエンドから直接 API を呼び出さない**
   - バックエンド経由でリクエスト

3. **定期的に API キーをローテーション**
   - 3～6ヶ月ごとに新しいキーを生成

4. **ログを監視**
   - API 使用状況を定期的に確認

---

**Brevo は個人ユーザーに優しいサービスです。住所なしでも、安心してメール配信を始められます。**
